services:
  - type: web
    name: bonziworld
    env: node
    buildCommand: |
      npm install -g grunt-cli
      npm install
      cd server && npm install
      grunt build
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000 